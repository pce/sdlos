# ── OS artifacts ─────────────────────────────────────────────────────────────
.DS_Store
._*
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini
ehthumbs.db
# Shader sources are committed.  Compiled bytecode lives in spirv/ and
# is typically excluded (see data/shaders/spirv/.gitignore).
