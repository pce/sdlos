#version 330
out vec4 fragColor;
uniform int weatherType;

void main() {
    float gradient = gl_FragCoord.y / 720.0;
    vec3 topColor;
    vec3 bottomColor;

    topColor = vec3(0.6, 0.8, 1.0);      // Sunshine
    bottomColor = vec3(1.0, 0.9, 0.5);

//     if (weatherType == 0) {
//         topColor = vec3(0.6, 0.8, 1.0);      // Sunshine
//         bottomColor = vec3(1.0, 0.9, 0.5);
//     } else if (weatherType == 1) {
//         topColor = vec3(0.2, 0.2, 0.4);      // Rain
//         bottomColor = vec3(0.3, 0.3, 0.3);
//     } else if (weatherType == 2) {
//         topColor = vec3(0.85, 0.85, 0.9);    // Snow
//         bottomColor = vec3(0.9, 0.9, 1.0);
//     } else if (weatherType == 3) {
//         topColor = vec3(0.2, 0.2, 0.2);      // Hail
//         bottomColor = vec3(0.4, 0.4, 0.5);
//     } else {
//         topColor = vec3(0.6, 0.4, 5.0);      // fallback
//         bottomColor = vec3(0.1, 0.1, 0.1);
//     }

    fragColor = vec4(mix(bottomColor, topColor, gradient), 1.0);
