// {$name$}.jade  —  generated by sdlos create  [minimal]
// Styles → {$name$}.css  (loaded automatically by jade_host)
//
// Layout hints
// ------------
// col / row  map to CSS flexbox column / row containers.
// Attribute names use camelCase; jade_host converts CSS kebab-case from the
// stylesheet by rules, till vendor prefix or is
// ideally: (background-color → backgroundColor, …).
// Inline attributes always override CSS class rules.

col(id="root" flexGrow="1")

  div.hero-title {$name$}
  div.hero-sub   Edit {$name$}.jade to get started.
